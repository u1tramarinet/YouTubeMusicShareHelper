<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <style name="Theme.YouTubeMusicShareHelper" parent="Theme.Material3.DynamicColors.DayNight">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <!-- Customize for material color design. -->
        <item name="elevationOverlayColor">@android:color/holo_red_light</item>
    </style>
</resources>