<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <style name="Theme.YouTubeMusicShareHelper" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <!-- Customize for material color design. -->
        <item name="elevationOverlayColor">@android:color/holo_red_light</item>
        <item name="bottomSheetStyle">@style/Theme.YouTubeMusicShareHelper.BottomSheetDialog</item>
    </style>

    <style name="Theme.YouTubeMusicShareHelper.BottomSheetDialog" parent="ThemeOverlay.MaterialComponents.DayNight.BottomSheetDialog">
        <item name="android:windowSoftInputMode">adjustResize</item>
        <item name="android:windowIsFloating">false</item>
    </style>
</resources>